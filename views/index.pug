extends layout

block content
  h1= title
  p Welcome to #[em Vietnamese Restaurant], a very basic restaurant website with categorized menu items, which you can create, read, update and delete(everyone is an admin).

  h2 Our Menu

  if error
    p Error getting menu content.
  else

    ul
      each category in categories
        li
          h3 #{category.name}
          p #{category.description}
          
          ul
            each item in items
              //categories are arrays
              if item.category[0].name == category.name
                li
                  h4 #{item.name}
                  p #{item.description}
                  p Price: #{item.price}
                  p Stock(today): #{item.numberinstock}

   

